{
  "name": "home-page",
  "version": "0.0.1",
  "description": "The personal and professional home page of Jayson Virissimo",
  "private": true,
  "scripts": {
    "build-html-resume": "echo 'Exporting to HTML...' && cd src/documents && resume export resume.html --format html --theme spartan",
    "build-pdf-resume": "echo 'Exporting to PDF...' && cd src/documents; resume export resume.pdf --theme ../../node_modules/jsonresume-theme-short",
    "build-resumes": "npm run validate && npm run build-html-resume && npm run build-pdf-resume",
    "deploy": "npm run test && git subtree push --prefix src origin master",
    "start": "browser-sync start --server ./src --files ./src",
    "test": "mocha test/*.spec.js",
    "validate": "echo 'Validating resume.json...' && cd src/documents && resume validate && echo 'Validated!'"
  },
  "author": "Jayson Virissimo <jayson.virissimo@asu.edu>",
  "devDependencies": {
    "browser-sync": "^2.29.0",
    "chai": "^4.5.0",
    "jsdom": "^25.0.0",
    "mocha": "^10.8.0",
    "resume-cli": "^3.1.2"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "jsonresume-theme-short": "^0.1.7",
    "jsonresume-theme-spartan": "^0.3.0"
  }
}
